import 'package:flutter/material.dart';

class add_event_page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Welcome to the Job Page'),
      ),
    );
  }
}